"""
CognitionOS Platform Assembly (v4).

This package is intentionally *not* named `platform` to avoid shadowing the
Python standard library module `platform` (used by third-party dependencies).
"""

__version__ = "4.0.0"
