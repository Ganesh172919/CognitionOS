# CognitionOS Frontend - Localhost Development
# Copy to .env.local and adjust as needed
# NEXT_PUBLIC_* vars are exposed to the browser

# Main API URL for localhost (API on port 8100)
# When set, dashboard/tasks/explain use main API paths.
NEXT_PUBLIC_API_URL=http://localhost:8100
